{% extends 'base.html.twig' %}
{% block title %}{{ parent() }} - Ajouter!{% endblock %}
{% block header %}
    {{ parent() }}
{% endblock %}
{% block body %}



    <table class="table table-danger text-center" style="margin-top: 50px">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user.id }}</td>
        </tr>
        <tr>
            <th>Firstname</th>
            <td>{{ user.firstname }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ user.name }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        </tbody>
    </table>
    <h2 class="card bg-danger mt-5"> Page de l'utilisateur {{ user.firstname }} {{ user.name }} </h2>

    <a href="{{ path('user_index') }}" role="button" class="btn btn-danger">Retour à la liste des Annonces</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}" role="button" class="btn btn-danger">Modifier</a>

    {{ include('user/_delete_form.html.twig') }}


{% endblock %}
{% block footer %}
    <h4 class="text-danger text-right">
        {{ copyright }}
    </h4>
{% endblock %}