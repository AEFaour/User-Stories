{% extends 'base.html.twig' %}
{% block title %}{{ parent() }} - View!{% endblock %}
{% block header %}
    {{ parent() }}
{% endblock %}
{% block body %}
    <h2>{{ advert.title }}</h2>
    <i>Par {{ advert.user }}, le {{ advert.createdAt|date('d/m/Y') }}</i>

    <div class="well">
        {{ advert.text }}
    </div>

    <p>
        <a href="{{ path('advert_index') }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Retour à la liste
        </a>
        <a href="#" class="btn btn-default">
            <i class="glyphicon glyphicon-edit"></i>
            Modifier l'annonce
        </a>
        <a href="#" class="btn btn-danger">
            <i class="glyphicon glyphicon-trash"></i>
            Supprimer l'annonce
        </a>
    </p>

{% endblock %}

{% block footer %}
    {{ parent() }}
{% endblock %}